# Contracts

Placeholder for subsystem 7 — Foundry workspace for `YurikaVault.sol` and `ShardToken.sol`. Will be scaffolded with the `yurika-foundry-contracts` skill when we get there.

For now: contract addresses in `.env` are blank, and the Django `domains/views.py` will use mock addresses through subsystems 2–6.
